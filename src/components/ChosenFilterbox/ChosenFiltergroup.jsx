import React, { Component } from 'react';

export default class ChosenFiltergroup extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return(
            /** Tmp to avoid errors */
            1
        );
    }
}